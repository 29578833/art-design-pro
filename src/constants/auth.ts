/** 回收订单审核通过（对应 /menus unique） */
export const AUTH_SCRAP_ORDER_AUDIT_APPROVE = 'scrap-order-audit-approve'

/** 回收订单审核驳回（对应 /menus unique） */
export const AUTH_SCRAP_ORDER_AUDIT_REJECT = 'scrap-order-audit-reject'
